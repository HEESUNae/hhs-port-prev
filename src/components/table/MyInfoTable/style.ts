import { styled } from 'styled-components';

export const StyledMyInfoTable = styled.div`
  table {
    margin: 1.5rem 0 2rem;
    th {
      min-width: 2rem;
      height: 1.8rem;
    }
    td {
      padding-left: 0.5rem;
    }
  }
`;
